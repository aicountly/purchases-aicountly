/**
 * Dashboard 3 — Suppliers.
 *
 * The screen itself lives in `../suppliers`, split into the panels it is made
 * of. This file is the entry point the dashboard container mounts, kept so the
 * five views are still found in one place.
 */

export { SupplierPerformance as SuppliersDashboard } from '../suppliers/SupplierPerformance'
